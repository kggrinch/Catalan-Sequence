# Catalan-Sequence

This project demonstrates the Catalan sequence through a light-weight recursive approach. The algorithm works well with small data set ranges because it was built to display the recursive nature of the sequence.
