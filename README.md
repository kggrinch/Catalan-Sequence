# Catalan-Sequence

This project demonstrates the Catalan sequence through a light-weight recursive approach. The algorithm is set to works with small data set ranges because it was built to display the recursive nature of the sequence.
